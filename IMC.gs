/** 
 * Função IMC
 * @param peso É o valor que representa o peso da pessoa
 * @param altura É o valor que representa a altura da pessoa
 * @return A função serve para indicar o Índice de Massa Corporal
 * @customFunction
 
*/
function IMC(peso, altura) {
  var resultado = peso/(altura^2);
return resultado
  
}
